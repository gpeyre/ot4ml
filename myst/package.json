{
  "private": true,
  "scripts": {
    "build": "myst build --html --execute",
    "jupyter": "jupyter lab --no-browser --port=8888 --ServerApp.token=ot4ml-local --ServerApp.allow_origin=http://localhost:3000 --ServerApp.disable_check_xsrf=True",
    "start": "myst start --execute"
  },
  "devDependencies": {
    "mystmd": "1.10.1"
  }
}
