{
  "private": true,
  "scripts": {
    "build": "myst build --html --execute",
    "jupyter": "jupyter lab --no-browser --port=8888 --ServerApp.token=ot4ml-local --ServerApp.allow_origin_pat='http://(localhost|127\\.0\\.0\\.1):300[0-9]' --ServerApp.disable_check_xsrf=True",
    "start": "myst start --execute"
  },
  "devDependencies": {
    "mystmd": "1.10.1"
  }
}
